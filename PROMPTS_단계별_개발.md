# 독립이사 후보자 POOL — 단계별 개발 프롬프트

> **사용법**
> 1. 각 단계 프롬프트를 **그대로 복사**해서 Claude Code에 붙여넣습니다.
> 2. 단계는 **순서대로** 진행합니다. 이전 단계가 끝나지 않은 상태에서 다음 단계로 넘어가지 마세요.
> 3. 모든 단계는 프로젝트 루트(`독립이사 후보자 POOL`)에서 실행하며, `PRD_독립이사_후보자_POOL.md`가 같은 폴더에 있어야 합니다.
> 4. 한 단계가 끝나면 `완료 기준(DoD)` 항목을 직접 확인한 뒤 다음 단계로 넘어갑니다.

---

## 공통 프리앰블 (모든 단계 프롬프트 앞에 이미 포함되어 있음)

```
프로젝트 루트의 PRD_독립이사_후보자_POOL.md를 먼저 처음부터 끝까지 읽고 작업을 시작해.
이 PRD가 유일한 요구사항 원천이다. PRD에 없는 기능을 임의로 추가하지 말고,
PRD와 충돌하는 판단이 필요하면 코드를 작성하기 전에 나에게 물어봐.

절대 원칙 (모든 단계 공통):
1. 실명 개인정보를 다루는 시스템이다. 개발/테스트에는 절대 실제 인물 데이터를 쓰지 말고
   합성 더미데이터만 사용한다.
2. 출처(source_id)가 없는 사실 데이터는 저장하지도, 화면에 표시하지도 않는다.
3. 검색 조건은 전부 드롭다운(selectbox/multiselect)이다. 키워드 텍스트 입력창을 만들지 않는다.
4. 도메인 로직은 core/ 에 두고 Streamlit UI(app.py, pages/)에서 분리한다.
   pages/ 안에는 비즈니스 로직과 SQL을 직접 쓰지 않는다.
5. 비밀정보(DB 접속정보, API 키)는 코드나 저장소에 하드코딩하지 않는다.
6. 작업이 끝나면 변경한 파일 목록과 실행 방법을 요약해서 알려줘.
```

---

# 1단계 — 기본 뼈대 작성

## 프롬프트 (복사해서 사용)

```
프로젝트 루트의 PRD_독립이사_후보자_POOL.md를 먼저 처음부터 끝까지 읽고 작업을 시작해.
이 PRD가 유일한 요구사항 원천이다. PRD에 없는 기능을 임의로 추가하지 말고,
PRD와 충돌하는 판단이 필요하면 코드를 작성하기 전에 나에게 물어봐.

[1단계 목표]
"독립이사 후보자 POOL" Streamlit 애플리케이션의 실행 가능한 뼈대(skeleton)를 만든다.
이번 단계에서는 화면이 열리고 더미데이터로 화면 간 이동이 되는 것까지가 목표다.
실제 데이터 수집, 분석 로직, 예쁜 디자인은 이번 단계에서 하지 않는다.

[해야 할 일]

1. 프로젝트 구조 생성 — PRD §6.9 (1)의 구조를 그대로 따를 것
   app.py
   pages/1_후보_검색.py, 2_후보_상세.py, 3_후보_비교.py, 4_POOL_관리.py,
         5_검수.py, 6_리포트.py, 7_관리자.py
   core/          도메인 로직 (search.py, scoring.py, screening.py, expertise.py, auth.py, constants.py)
   data/          DB 접근 계층 (models.py, repository.py, session.py, seed.py)
   collectors/    수집기 (이번 단계는 인터페이스와 빈 구현만)
   reports/       PDF/XLSX 생성 (이번 단계는 인터페이스만)
   tests/
   .streamlit/config.toml
   .streamlit/secrets.toml.example   ← 예시 파일만. 실제 secrets.toml은 .gitignore 처리
   requirements.txt, README.md, .gitignore, .env.example

2. 데이터 모델 구현 — PRD §7 논리 스키마를 SQLAlchemy 모델로 구현
   Person, Position, Directorship, Expertise, Reputation, Achievement,
   ScreeningResult, Source, Pool, PoolMember, ReviewLog, AuditLog, CodeMaster,
   AppUser, ShareLink, AccessLog
   - 모든 사실 테이블(Position, Directorship, Expertise, Reputation, Achievement)은
     source_id를 NOT NULL 외래키로 둘 것 (PRD F-05-1: 출처 없는 값 저장 금지)
   - Directorship.remaining_term_months는 저장 컬럼이 아니라 계산 프로퍼티로 구현
   - 개발 단계 DB는 SQLite, 운영은 PostgreSQL로 전환 가능하도록 접속 문자열만 환경변수로 분리

3. CodeMaster(드롭다운 코드) 초기 적재
   PRD §6.1의 모든 필터 선택지와 §6.4의 전문분야 분류체계(대분류 15개 + 중분류 전체)를
   CodeMaster 테이블에 시드 데이터로 넣는다. 코드값은 하드코딩하지 말고 전부 DB에서 읽어온다.

4. 합성 더미데이터 생성 (data/seed.py)
   - 가상의 후보자 200명. 실존 인물과 겹치지 않도록 이름은 명백한 가공명으로 생성
     (예: "후보자001 김OO" 형태 또는 Faker 한국어 로케일 사용)
   - 1인당: 기본정보 + 과거 경력 3~8건 + 현재 타사 등기임원 0~3건 +
     전문분야 1~3개 + 평판 0~3건 + 업적 1~4건 + 출처 레코드
   - 일부는 임기 만료 임박(6개월 이내), 일부는 겸직 3개 이상(오버보딩),
     일부는 스크리닝 경고/결격 상태가 되도록 의도적으로 섞을 것
   - 모든 URL은 example.com 기반의 가짜 URL로 채운다

5. 인증·권한 골격 (core/auth.py)
   - 이번 단계는 실제 SSO 연동 대신 "역할 선택 모의 로그인"으로 구현하되,
     인터페이스는 나중에 OIDC로 교체 가능하도록 추상화할 것
   - 역할: 담당자 / 사무국장 / 법무 / 관리자 / 외부뷰어 (PRD §3.1, F-09-11)
   - require_login(), require_role(*roles) 데코레이터 또는 가드 함수를 만들고
     모든 pages/ 최상단에서 호출한다 (PRD F-09-11: 메뉴 숨김만으로는 불충분)
   - 로그인하지 않은 상태에서 페이지 URL 직접 접근 시 데이터가 보이지 않아야 한다

6. 각 페이지 뼈대 구현 (기능은 최소, 이동과 데이터 흐름만 확인)
   - 1_후보_검색: PRD §6.1 필터를 전부 드롭다운으로 배치(동작은 단순 WHERE 필터 수준),
                  최대 조회 인원 수 드롭다운(F-01-8) 포함, 결과를 st.dataframe으로 표출
   - 2_후보_상세: PRD §6.3 (1)~(7)의 섹션 제목과 뼈대만 배치, 더미데이터 표시
   - 3_후보_비교 / 4_POOL_관리 / 5_검수 / 6_리포트 / 7_관리자: 화면 틀과 "준비 중" 표시
   - 사이드바에 현재 사용자·역할 표시, 로그아웃 버튼

7. 기본 인프라
   - st.session_state 기반 상태 관리 유틸 (core/state.py)
   - @st.cache_resource로 DB 세션, @st.cache_data(ttl)로 코드 조회 캐싱
     ※ 사용자 종속 데이터는 캐시하지 말 것 (PRD F-09-7)
   - AccessLog 기록 헬퍼 (core/audit.py) — 로그인/페이지 조회 시 기록
   - tests/ 에 core 모듈 단위테스트 뼈대 (pytest)

[하지 말 것]
- 실제 DART API 호출, 뉴스 크롤링 (2단계에서 함)
- 전문분야 자동 분류, 평판 분석, 적합도 점수 실제 로직 (2단계)
- CSS 커스터마이징, 테마 디자인 (3단계)
- 실존 인물 데이터 사용

[완료 기준 (DoD)]
1. `streamlit run app.py`로 앱이 에러 없이 실행된다.
2. 모의 로그인 후 7개 페이지를 모두 이동할 수 있다.
3. 로그아웃 상태에서 각 페이지에 직접 접근하면 데이터가 노출되지 않는다.
4. 후보 검색 화면의 모든 드롭다운 선택지가 CodeMaster(DB)에서 로드된다.
5. 더미 후보 200명이 조회되고, 최대 조회 인원 수 선택이 동작한다.
6. `pytest` 실행 시 뼈대 테스트가 통과한다.
7. README.md에 설치·실행·시드데이터 생성 방법이 정리되어 있다.

마지막에 생성한 파일 트리와 실행 방법을 요약해서 알려줘.
```

---

# 2단계 — 기능 추가

## 프롬프트 (복사해서 사용)

```
프로젝트 루트의 PRD_독립이사_후보자_POOL.md를 다시 읽고, 1단계에서 만든 뼈대 위에 기능을 구현해.
PRD가 유일한 요구사항 원천이다. PRD에 없는 기능을 임의로 추가하지 마.

[2단계 목표]
PRD §6의 기능 요구사항(F-01 ~ F-09)을 실제로 동작하게 만든다.
디자인은 아직 손대지 않는다. 기능이 정확히 동작하는 것이 목표다.

[해야 할 일 — PRD 기능 ID 기준]

A. 검색 (F-01)
 - 17개 필터 전체 동작. 미선택 필터는 조건에서 제외 (F-01-1)
 - 직업 대분류 → 중분류 cascading, 상위 변경 시 하위 초기화 (F-01-2, F-09-3)
 - 각 드롭다운 옵션 옆 후보 건수 배지 (F-01-3)
 - 선택 조건 chip 표시 + 개별 해제 (F-01-4)
 - 검색 프리셋 저장/불러오기 (F-01-5)
 - 결과 0건 시 조건 완화 제안: 어떤 필터를 빼면 몇 명이 되는지 계산해서 제시 (F-01-6)
 - 최대 조회 인원 수 (F-01-8)
   · 단순 절단이 아니라 "정렬 기준 적용 후 상위 N명"
   · SQL LIMIT으로 DB 단에서 제한, 전체 건수는 별도 COUNT 쿼리 (F-09-5-1)
   · `전체 매칭 N명 중 상위 M명 표시` 배너 항상 노출
   · `전체` 선택 시에도 시스템 상한(관리자 설정, 기본 500) 적용 + 안내 문구
   · 역할별 상한(외부뷰어 50명) 적용
   · 최근 선택값을 사용자별로 기억

B. 검색 결과 (F-02)
 - 목록 컬럼 전체 구현(성명/성별/나이/현재직업/전문분야/겸직수/스크리닝/적합도/최신성/액션)
 - 최대 4인 비교 보기 (F-02-1)
 - POOL 저장 (F-02-2)
 - XLSX 내보내기 — 출처 URL 컬럼 + 조회 조건·인원수·일시 기록 (F-02-3)
 - 페이지네이션(기본 50건)과 조회 인원 수(N)를 별개 개념으로 구현 (F-02-5)

C. 상세 프로파일 (F-03)
 - (1)~(7) 전 섹션 구현
 - 과거 경력: 최근 10년 + 임원급 이상만 필터링. 판정 규칙을 core/에 명시적으로 구현.
   10년 초과라도 장·차관급 등은 `주요 경력 하이라이트`로 별도 표기
 - 타사 등기임원: 잔여 임기 = 임기만료일 - 오늘 → "O년 O개월", 6개월 이내 강조 (F-03-1)
 - 겸직 한도 초과 시 오버보딩 경고 (F-03-2), 이사회 출석률 표시 (F-03-3)
 - 종료된 등기임원 이력은 st.expander로 접기 (F-03-4)
 - 평판: 확인된 사실만. 미확인 건은 "사실관계 미확인"으로 구분 표기하고 점수 미반영
 - (7) 고려사항 18개 체크리스트 — 자동 판정 항목은 신호등, 수기 항목은 입력란 + 코멘트/첨부

D. 전문분야 자동 분류 (F-04)
 - 택소노미 코드로만 부여, 자유 텍스트 금지
 - 1차: 규칙 기반 키워드 사전(core/expertise_rules.py) → 2차: LLM 분류(선택적, 플래그로 on/off)
 - 최신성 가중(최근5년 1.0 / 6~10년 0.6 / 10년 초과 0.3) × 출처 신뢰도 가중 적용
 - 상위 3개 대표 + 나머지 보조, 신뢰도(상/중/하)와 근거 건수 표시
 - **근거 스니펫(원문 인용 + URL)이 없는 분류는 출력 금지** — 이 가드를 코드로 강제할 것
 - 담당자 수정·확정 기능 및 수정 이력 저장

E. 결격·리스크 스크리닝 (§5.1, F-06)
 - R-01 ~ R-08 룰을 core/screening.py에 룰 엔진 형태로 구현
 - **임계값(겸직 한도, 재직 연수 상한, 냉각기간 연수 등)은 하드코딩 금지.**
   RuleConfig 테이블/설정에서 읽어오고 관리자 화면에서 수정 가능해야 한다 (PRD §5 상단 경고)
 - 판정 결과: pass / warn / fail → 🟢🟡🔴, 사유와 근거 데이터 함께 저장
 - 🔴 후보는 적합도와 무관하게 목록 하단 분리 표시 (F-06)

F. 적합도 점수 (F-06)
 - 전문분야 매칭 40 + 스킬갭 보완 20 + 경력수준 15 + 가용성 15 - 리스크 10
 - 가중치는 관리자 설정값으로 분리
 - 화면에 "보조 지표이며 최종 판단은 담당자·법무 검토로 확정" 문구 상시 노출

G. 데이터 수집 (F-05)
 - collectors/dart.py: DART OpenAPI로 임원현황 수집 → Person/Position/Directorship/Source 적재
   · API 키는 환경변수. rate limit·재시도·에러 로깅 처리
 - collectors/news.py: 뉴스 API 연동 인터페이스 (키 미발급 시 비활성)
 - collectors/web.py: 기업 홈페이지 수집 — robots.txt 확인 로직 필수
 - **소스 화이트리스트 강제**: 익명 커뮤니티/SNS/블로그/위키 도메인은 수집 단계에서 차단 (F-05 제외 목록)
 - 신뢰도 등급(A/B/C) 부여, 충돌 시 상위 등급 채택 + 하위는 `대체 정보`로 병기 (F-05-3)
 - 원문 스냅샷 저장 (F-05-6), URL 생존 점검 배치 (F-05-5)
 - 동명이인 판별: 생년월 + 소속 이력 복합 매칭. 신뢰도 낮으면 자동 결합 금지하고
   수기 확인 큐로 보낸다 (PRD §11 리스크)
 - 수집은 Streamlit 프로세스가 아니라 별도 배치 스크립트로 실행 (F-09-8)

H. POOL 관리 (F-07) / 검수 (F-08)
 - POOL CRUD, 상태 전이(후보등록→1차검토→법무검증→사추위보고→접촉→최종후보/보류/제외)
 - 후보 제외 시 사유 필수 입력
 - 검수 화면 3분할: 자동값 | 원문 근거 | 승인·수정·삭제
 - **미검수 프로파일은 PDF/XLSX 출력 차단** (F-08-1, F-09-9)
 - 수동 수정 항목은 자동 갱신 시 덮어쓰지 않고 `충돌 알림` (F-08-4)

I. 리포트
 - 사추위 보고용 2페이지 PDF — PRD 부록 A 템플릿 그대로
 - 항목별 출처 각주(부록 B 형식), 열람자·일시 워터마크, 대외비 고지

J. 인증·권한·공유 (F-09 (2)(3))
 - 모의 로그인을 OIDC 연동 가능한 구조로 정리(프로바이더 인터페이스 + 모의 구현)
 - 역할별 페이지·기능 접근 제어를 각 페이지 최상단에서 재검증
 - 외부뷰어: 지정 POOL만 읽기 전용, 다운로드 차단, 접근 만료일 기본 30일 (F-09-12)
 - 세션 유휴 30분 자동 로그아웃 (F-09-13)
 - 공유 링크 (F-09-14~18)
   · POOL ID + 만료일시 + 수신자 이메일에 바인딩된 토큰, 토큰 단독 열람 불가(로그인 필수)
   · 토큰은 해시로 저장, 발급 시 수신자/목적/범위/만료일 입력 필수
   · 즉시 회수(revoke) → 다음 요청부터 차단
   · S-09 공유 관리 화면
 - AccessLog: 로그인/검색/조회/다운로드/공유발급·회수 전부 적재 (F-09-22)

[제약]
- pages/ 안에 SQL이나 비즈니스 로직을 직접 쓰지 말 것. 전부 core/ + data/repository 경유.
- 캐시 키에 사용자 ID를 포함하거나 사용자 종속 데이터는 캐시하지 말 것 (F-09-7)
- 실제 데이터 수집은 더미 모드 / 실제 모드를 설정 플래그로 전환 가능하게 할 것.
  기본값은 더미 모드.

[완료 기준 (DoD)]
1. PRD §13 인수 기준 1~9번, 15~17번이 실제로 동작한다.
2. 🔴 결격 후보가 목록 상단에 노출되지 않는다.
3. 미검수 프로파일의 PDF 출력 버튼이 비활성이다.
4. 근거 스니펫 없는 전문분야가 단 하나도 표시되지 않는다(테스트로 검증).
5. core/ 모듈 단위테스트 커버리지 70% 이상, pytest 전부 통과.
6. 수집 배치를 더미 모드로 1회 실행해 데이터가 정상 적재된다.

각 기능이 PRD의 어느 요구사항 ID를 구현한 것인지 코드 주석 또는 커밋 메시지에 남겨줘.
```

---

# 3단계 — UI 디자인

## 프롬프트 (복사해서 사용)

```
프로젝트 루트의 PRD_독립이사_후보자_POOL.md를 참고해서, 2단계까지 구현된 기능의 UI를 정리해.

[3단계 목표]
"이사회 사무국 담당자가 실제 업무에서 매일 쓰는 사내 시스템"에 맞는 화면을 만든다.
화려함이 아니라 **판단 속도와 오해 방지**가 목적이다. 기능 동작은 바꾸지 말고 표현만 개선한다.

[디자인 원칙]
1. 이 화면의 결과물은 사추위·이사회에 올라가는 자료다. 신뢰감 있고 절제된 톤으로.
   장식적 요소, 과한 색상, 이모지 남발 금지.
2. 색은 의미 전달에만 쓴다: 🔴결격 / 🟡확인필요 / 🟢이슈없음, 임기 임박 강조 정도.
   그 외에는 무채색 + 포인트 컬러 1개.
3. 담당자가 **놓치면 안 되는 정보**는 시각적으로 가장 강하게: 결격 판정, 오버보딩 경고,
   임기 6개월 이내, "전체 N명 중 상위 M명 표시" 배너, 미검수 상태.
4. 모든 수치·사실 옆에는 출처 접근 경로가 항상 보여야 한다. 출처를 찾으려고 헤매게 하지 말 것.
5. 1280px 이상에서 정상 동작. 인쇄(PDF 출력) 시 레이아웃이 깨지지 않을 것.

[해야 할 일]

A. 전역 스타일
 - .streamlit/config.toml에 테마 정의(primaryColor, backgroundColor, textColor, font)
 - 공통 CSS를 core/ui/style.py 한 곳에서 주입. 페이지마다 흩어놓지 말 것
 - 공통 헤더: 시스템명 / 현재 사용자·역할 / 대외비 표시
 - 사이드바: 네비게이션 + 사용자 정보 + 로그아웃. 역할에 따라 메뉴 노출 제어

B. 재사용 UI 컴포넌트 (core/ui/components.py)
 - status_badge(): 🟢🟡🔴 스크리닝 배지
 - source_popover(): 값 옆 [출처] → 발행처·문서명·발행일·URL·인용 스니펫
 - metric_card(): 요약 지표 카드
 - candidate_card(): 후보 요약 카드
 - term_gauge(): 잔여 임기 시각화(6개월 이내 강조)
 - confidential_banner(): 대외비/워터마크 고지
 - result_count_banner(): "전체 매칭 N명 중 상위 M명 표시"

C. 화면별 개선
 - S-00 로그인: 단순·명확. 대외비 고지와 접근 권한 안내
 - S-01 대시보드: 내 POOL 현황 / 임기 만료 임박 자사 이사 / 최근 갱신 알림 / 검수 대기 건수
 - S-02 검색: 좌측 필터 패널(접기 가능, 그룹핑) + 우측 결과. 조건 chip을 상단에 고정.
   최대 조회 인원 수 드롭다운은 결과 영역 상단에 배치해 결과를 보며 즉시 변경 가능하게.
   결격 후보는 구분선 아래 별도 섹션으로 분리
 - S-03 비교: st.columns 기반 4열, 항목명 좌측 고정, 차이나는 값 강조
 - S-04 상세: 상단 요약 헤더(이름·나이·현직·스크리닝·적합도) 고정,
   본문은 탭 또는 앵커로 (1)~(7) 섹션 구분. 경력 타임라인은 Plotly 또는 Altair
 - S-05 POOL: 상태별 칸반 형태 보드
 - S-06 검수: 좌(자동값)·중(원문근거)·우(승인/수정) 3분할, 미검수 항목 시각적 구분
 - S-07 리포트: 미리보기 + 다운로드. 검수 미완료 시 버튼 비활성 사유를 명시
 - S-08 관리자 / S-09 공유 관리: 표 중심. 만료 임박·회수 가능 상태 명확히

D. 상태·피드백
 - 로딩 시 st.spinner, 100명 이상 조회 시 지연 안내
 - 빈 상태(0건)에는 조건 완화 제안을 함께 표시
 - 에러는 사용자 언어로. 스택 트레이스 노출 금지

E. 차트
 - Plotly 또는 Altair 사용. 색상은 전역 팔레트와 통일
 - 경력 타임라인, 평판 논조 추이, 검색 결과 성별·연령 분포(다양성 확인용)

[하지 말 것]
- 기능·데이터 로직 변경 (버그를 발견하면 고치지 말고 목록으로 보고만 할 것 → 4단계에서 처리)
- 외부 CSS/JS CDN 의존 추가
- 검색 조건을 텍스트 입력으로 바꾸는 등 PRD 원칙에 어긋나는 변경

[완료 기준 (DoD)]
1. 전 화면이 통일된 테마로 보이고, 색상이 의미 전달 용도로만 쓰인다.
2. 결격·오버보딩·임기임박·미검수·결과 절단 배너가 한눈에 인지된다.
3. 모든 사실 항목 옆에 출처 접근 UI가 있다.
4. 1280px에서 가로 스크롤 없이 동작한다.
5. 스타일 코드가 core/ui/ 한 곳에 모여 있다.

작업 후 주요 화면 구성 변경 사항을 화면별로 요약하고,
작업 중 발견했지만 고치지 않은 기능 이슈가 있으면 별도 목록으로 알려줘.
```

---

# 4단계 — PRD 부합성 및 버그 검토

## 프롬프트 (복사해서 사용)

```
프로젝트 루트의 PRD_독립이사_후보자_POOL.md 전체를 다시 읽고,
현재 구현된 코드가 PRD를 충족하는지, 버그는 없는지 검토해.

[4단계 목표]
검토와 보고가 목적이다. **먼저 전수 검토하고 보고서를 작성한 뒤, 내 승인을 받고 수정한다.**
검토 없이 바로 코드를 고치지 마.

[Part 1 — PRD 요구사항 추적 매트릭스]
PRD의 모든 요구사항 ID를 표로 만들고 구현 여부를 판정해.
대상: F-01-1~8, F-02-1~6, F-03-1~6, F-04-1~4, F-05-1~6, F-06, F-07, F-08-1~4,
      F-09-1~23, R-01~R-08(§5.1), §5.2 개인정보 요건, §10 비기능 요구사항

| 요구사항 ID | 내용 요약 | 구현 상태 | 근거 파일:라인 | 비고 |
|---|---|---|---|---|
구현 상태는 다음 중 하나로만: 충족 / 부분충족 / 미구현 / 요구사항과 다르게 구현

**중요: "아마 됐을 것"으로 판정하지 말 것. 실제 코드를 열어 확인하고 파일:라인을 근거로 제시해.
확인 못 한 항목은 '확인 불가'로 정직하게 표기해.**

[Part 2 — 인수 기준 검증]
PRD §13의 인수 기준 1~17번을 하나씩 실제로 검증해.
가능한 것은 테스트 코드나 스크립트로 자동 검증하고, 수동 확인이 필요한 항목은 재현 절차를 적어줘.
특히 다음은 반드시 실제로 실행해서 확인:
 - #3  과거 경력이 최근 10년 + 임원급만 표시되는가 (경계값: 정확히 10년 전, 9년11개월)
 - #4  잔여 임기 계산 (월말/윤년/이미 만료된 건/임기만료일 미상)
 - #5  출처 URL 누락 0건 (전수 쿼리로 검증)
 - #7  결격 후보 상단 미노출
 - #8  미검수 프로파일 PDF 출력 차단
 - #10 로그아웃 상태로 모든 페이지 URL 직접 접근
 - #11 외부뷰어 권한 우회 시도 (URL 직접 입력, 파라미터 조작)
 - #12 공유 링크 회수·만료 즉시 반영
 - #15 "전체 매칭 N명 중 상위 M명" 표기 정확성 (N=M일 때, 매칭 0건일 때)
 - #16 전체 선택 시 시스템 상한 적용
 - #17 100명 조회 시 3초 이내

[Part 3 — 보안 점검]
PRD 부록 C 체크리스트 12개 항목을 전부 점검하고 통과/실패를 판정해.
추가로 다음을 확인:
 - SQL 인젝션 가능 지점 (문자열 포맷으로 쿼리를 만드는 곳)
 - st.cache_data 캐시 키에 사용자 컨텍스트가 빠져 타 사용자 데이터가 새는 곳
 - 세션 상태를 신뢰해 권한을 판단하는 곳 (클라이언트 조작 가능성)
 - 저장소 내 하드코딩된 비밀정보, .gitignore 누락
 - 에러 메시지·로그에 개인정보나 스택 트레이스가 노출되는 곳

[Part 4 — 버그 헌팅]
다음 관점으로 코드를 훑고 실제 재현 가능한 버그를 찾아줘.
추측성 지적이 아니라 **재현 경로(입력 → 잘못된 결과)** 를 제시할 수 있는 것만 보고할 것.
 1. 경계값: 검색 결과 0건 / 1건 / 상한 초과, 나이·임기 경계, 데이터 NULL
 2. 날짜: 임기 만료 계산, 10년 경력 필터, 타임존, 과거 날짜, 미상 처리
 3. 상태 관리: 페이지 이동 후 st.session_state 잔존·오염, 필터 cascading 초기화 누락,
    프리셋 불러오기 시 상태 충돌
 4. 캐시: 데이터 갱신 후 캐시 미무효화로 옛 값 표시
 5. 권한: 역할 전환 시 이전 역할 데이터 잔존
 6. 데이터 정합성: source_id 없는 레코드 생성 가능 경로, 동명이인 오결합,
    근거 없는 전문분야가 새어나오는 경로
 7. 예외 처리: DART/뉴스 API 실패, DB 연결 끊김, 잘못된 후보 ID 접근
 8. 동시성: 두 사용자가 같은 POOL·프로파일을 동시 수정할 때
 9. 수치 로직: 적합도 점수 가중치 합계, 스크리닝 룰 우선순위, 정렬 안정성

각 버그는 다음 형식으로:
| # | 심각도 | 위치(파일:라인) | 증상 | 재현 경로 | 원인 | 수정 방안 |
심각도: Critical(개인정보 유출·잘못된 결격 판정) / High / Medium / Low

[Part 5 — 종합 보고서]
위 Part 1~4를 REVIEW_보고서.md 파일로 저장하고, 마지막에 다음을 정리해:
 - PRD 충족률 (충족 / 전체 요구사항 수)
 - 운영 배포 전 반드시 고쳐야 할 항목 (Critical + 보안 실패 항목)
 - 고치면 좋은 항목
 - PRD 자체를 수정해야 할 항목 (요구사항이 모순되거나 현실적으로 불가능한 경우)

[진행 방식]
1. 먼저 Part 1~5를 완료하고 REVIEW_보고서.md를 보여줘. 이 시점에서 멈춘다.
2. 내가 수정 범위를 지정하면 그때 코드를 고친다.
3. 수정 후에는 수정한 항목별로 재검증 결과를 보고한다.

검토 과정에서 판단이 애매한 부분은 '이건 PRD 해석이 갈린다'고 명시하고 나에게 물어봐.
```

---

## 부록. 단계 간 체크포인트

| 단계 | 넘어가기 전 확인 | 확인 방법 |
|---|---|---|
| 1 → 2 | 앱 실행, 7개 페이지 이동, 비로그인 차단, 드롭다운 DB 로드 | 직접 실행 + URL 직접 접근 테스트 |
| 2 → 3 | 인수 기준 1~9·15~17 동작, pytest 통과 | `pytest` + 화면 수동 확인 |
| 3 → 4 | 전 화면 테마 통일, 경고 요소 인지성 | 화면 캡처 비교 |
| 4 → 배포 | 부록 C 12개 항목 전부 통과, Critical 0건 | REVIEW_보고서.md |

## 부록. 추가로 쓸 만한 보조 프롬프트

| 상황 | 프롬프트 |
|---|---|
| 특정 기능만 다시 | `PRD의 F-01-8(최대 조회 인원 수) 요구사항만 다시 읽고, 현재 구현이 "상위 N명" 방식인지 단순 절단인지 확인한 뒤 PRD대로 고쳐줘.` |
| 법령 파라미터 점검 | `PRD §5.1의 R-01~R-08 룰에서 하드코딩된 임계값이 있는지 전수 확인하고, 전부 설정값으로 분리해줘. PRD §5 상단에 하드코딩 금지가 명시돼 있다.` |
| 성능 문제 | `조회 인원 수 200명 선택 시 응답이 느리다. 쿼리 실행 계획과 캐시 적용 지점을 분석하고, PRD §10 성능 요구(3초 이내)를 만족하도록 개선해줘.` |
| 배포 직전 | `PRD 부록 C 배포 전 보안 점검 체크리스트 12개 항목을 하나씩 실제로 검증하고 통과/실패를 근거와 함께 보고해줘. 실패 항목은 수정 방안까지.` |
| 운영 전환 | `개발용 SQLite를 PostgreSQL로 전환하고, Docker Compose(앱 + DB + Nginx 리버스 프록시)와 배포 문서를 작성해줘. PRD §6.9 (4)와 §8을 따를 것.` |
