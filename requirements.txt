streamlit>=1.40,<2.0
SQLAlchemy>=2.0,<3.0
pandas>=2.0
python-dotenv>=1.0
pytest>=8.0
pytest-cov>=5.0
requests>=2.31
openpyxl>=3.1       # XLSX 내보내기 (PRD F-02-3)
reportlab>=4.0      # 사추위 보고용 PDF (PRD 부록 A)

# 운영 전환 시 활성화 (PRD §8)
# psycopg[binary]>=3.1

# 3단계에서 사용 (차트)
# plotly>=5.20
