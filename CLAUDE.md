# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## ⚠️ 세션 시작 시 가장 먼저 (개발 완료 시까지 유효)

1. **`PROGRESS.md`를 먼저 읽고 `▶ 다음 작업`부터 바로 이어간다.** PRD·코드 전체를 처음부터 다시 분석하지 않는다(토큰 절약). 설계 결정은 `PROGRESS.md`에 있는 대로 따른다.
2. `git status`로 마지막 커밋 이후 미완성 변경을 확인하고 그 작업 단위를 마저 끝낸다.
3. 작업은 작은 단위로 끊는다. 단위마다 **테스트 통과 → `PROGRESS.md` 갱신 → `git commit`**. 사용량 한도로 언제 끊겨도 잃는 것이 한 단위 이하가 되게 한다.
4. 새 설계 결정·사용자 확인 사항이 생기면 즉시 `PROGRESS.md`에 적는다(대화에만 남기지 않는다).

## 저장소 현재 상태

**1단계(기본 뼈대) 완료.** 다음 작업은 `PROMPTS_단계별_개발.md`의 2단계(기능 추가)다. 설치·실행 방법은 `README.md` 참고.

| 파일 | 역할 |
|---|---|
| `PRD_독립이사_후보자_POOL.md` | **유일한 요구사항 원천.** 코드 작업 전 반드시 읽을 것 |
| `PROMPTS_단계별_개발.md` | 4단계 구현 순서(뼈대 → 기능 → UI → 검토)와 단계별 지시 |

구현 요청을 받으면 임의로 시작하지 말고 `PROMPTS_단계별_개발.md`의 해당 단계 정의를 따른다. 단계를 건너뛰지 않는다.

## 무엇을 만드는 시스템인가

상장회사 이사회 사무국이 **독립이사(사외이사) 후보자 POOL**을 만드는 사내 도구. DART·뉴스·기업 홈페이지 등 공개 데이터를 수집·구조화하고, 드롭다운 조건 검색으로 후보를 찾아 사추위 보고자료까지 만든다. Streamlit 웹앱으로 배포하고 인가된 관계자에게 공유한다.

**이 시스템은 실명 개인정보를 대량으로 다룬다.** 잘못된 결격 판정이나 미확인 부정 정보 기재는 개인의 법적·평판 피해로 직결된다. 아래 불변 규칙은 편의를 위해 완화하지 않는다.

## 불변 규칙 (위반 시 되돌릴 것)

PRD 여러 절에 흩어져 있으나 전부 필수다.

1. **출처 없는 사실 데이터는 저장도 표시도 금지** (F-05-1). `Position` / `Directorship` / `Expertise` / `Reputation` / `Achievement`는 `source_id`를 NOT NULL로 둔다. 우회 경로가 생기면 그것이 버그다.
2. **근거 스니펫(원문 인용 + URL) 없는 전문분야는 출력 금지** (F-04-2, 분류 로직 5). LLM 환각 차단 가드이므로 코드로 강제한다.
3. **검색은 드롭다운 전용.** `st.text_input` 기반 키워드 검색을 만들지 않는다 (F-01, F-09-1). 드롭다운 선택지는 하드코딩하지 않고 `CodeMaster` 테이블에서 로드한다.
4. **법령 관련 임계값 하드코딩 금지** (PRD §5 상단). 겸직 한도, 재직 연수 상한, 냉각기간 등은 설정값으로 분리하고 관리자 화면에서 수정 가능해야 한다. 법 개정이 잦아 코드에 박으면 결격 후보를 추천하게 된다.
5. **익명 접근 차단.** 모든 `pages/` 최상단에서 권한을 재검증한다. 메뉴 숨김만으로는 불충분하다 (F-09-11).
6. **실명 데이터로 개발·테스트하지 않는다.** 합성 더미데이터만 사용하고, Streamlit Community Cloud 등 퍼블릭 환경에는 실명 데이터를 절대 배포하지 않는다 (§6.9 (4)).
7. **소스 화이트리스트.** 익명 커뮤니티·SNS·개인 블로그·위키는 수집 단계에서 차단한다 (F-05 제외 목록). 명예훼손 리스크 통제 장치다.
8. **미검수 프로파일은 외부 출력 차단** (F-08-1, F-09-9).

## 계획된 아키텍처

```
collectors/   DART OpenAPI · 뉴스 API · 웹 크롤러  → Streamlit 프로세스 밖 배치로 실행
     ↓
data/         SQLAlchemy 모델 + repository (개발 SQLite / 운영 PostgreSQL)
     ↓
core/         도메인 로직: search, scoring, screening, expertise, auth, audit, state
     ↓
app.py + pages/   Streamlit 멀티페이지 UI
```

**계층 규칙**: `pages/` 안에 SQL이나 비즈니스 로직을 직접 쓰지 않는다. 전부 `core/` + `data/repository` 경유. Streamlit 종속을 UI 계층에 가두어 두어야 향후 별도 프론트엔드로 전환할 수 있고, `core/`를 단위테스트할 수 있다.

**무거운 작업은 Streamlit에서 분리** (F-09-8). Streamlit은 상태 변경마다 스크립트를 재실행하므로, 수집·분석을 앱 안에서 돌리면 사용성이 무너진다. 화면은 진행 상태만 조회한다.

**캐시 주의** (F-09-7): `@st.cache_resource`는 DB 커넥션, `@st.cache_data(ttl)`은 코드값 등 공용 데이터에만. 사용자별 권한이 다른 데이터는 캐시하지 않거나 캐시 키에 사용자 ID를 포함한다 — 캐시를 통한 권한 우회가 실제 위험이다.

## 구현 시 자주 틀리는 도메인 로직

- **과거 경력 필터** (F-03 (3)): 최근 10년 **이내** + **임원급 이상**만. 경계값(정확히 10년 전)과 "10년 초과지만 장·차관급" 예외(`주요 경력 하이라이트`로 별도 표기)를 함께 처리한다.
- **잔여 임기** (F-03-1): `임기만료일 - 오늘` → `O년 O개월`. 이미 만료된 건, 임기만료일 미상, 윤년을 모두 다뤄야 한다.
- **최대 조회 인원 수** (F-01-8): 단순 절단이 아니라 **정렬 기준 적용 후 상위 N명**. SQL `LIMIT`으로 DB 단에서 제한하고, 전체 건수는 별도 `COUNT`로 조회해 `전체 매칭 N명 중 상위 M명 표시` 배너에 쓴다. 담당자가 결과가 잘린 걸 모르고 지나치는 것이 이 기능의 핵심 리스크다.
- **결격(🔴) 후보** (F-06): 적합도 점수와 무관하게 목록 하단으로 분리 표시.
- **출처 충돌** (F-05-3): 신뢰도 등급 A(공시) > B(기관 공식) > C(언론). 상위 등급을 채택하되 하위 값을 `대체 정보`로 병기해 충돌 사실을 숨기지 않는다.
- **평판** (F-03 (5)): 확인된 사실만 기재. 미확인 건은 "사실관계 미확인"으로 구분 표기하고 자동 점수에 반영하지 않는다.
- **동명이인**: 생년월 + 소속 이력 복합 매칭. 신뢰도가 낮으면 자동 결합하지 말고 수기 확인 큐로 보낸다.

## 명령어

```bash
pip install -r requirements.txt
python -m data.seed              # 합성 더미데이터 생성 (--reset: 전체 재생성)
streamlit run app.py             # 앱 실행
pytest                           # 전체 테스트 (임시 SQLite 사용, 개발 DB 무관)
pytest tests/test_pages_guard.py -v    # 파일 단위
```

`.venv`의 Python으로 실행한다. 전역 Python에서는 `langsmith` pytest 플러그인이 기동을 막으므로 `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1`이 필요하다.

## 기술 스택 (PRD §8 확정안)

Python 3.11+ / Streamlit / SQLAlchemy / PostgreSQL(운영) · SQLite(개발) / APScheduler 또는 Airflow(배치) / Plotly·Altair / ReportLab 또는 WeasyPrint(PDF) / Docker / Nginx + OAuth2 Proxy(SSO)

## 문서 갱신 규칙

- 요구사항이 바뀌면 코드보다 `PRD_독립이사_후보자_POOL.md`를 먼저 고치고 버전·변경 이력을 남긴다. PRD가 원천이지 코드가 원천이 아니다.
- PRD가 모순되거나 현실적으로 불가능하다고 판단되면 임의 해석하지 말고 사용자에게 확인한다.
- PRD §14 미결 사항 16건은 법무·IT·사무국 결정 대기 항목이다. 여기에 해당하는 값(공유 링크 만료 기간, 조회 상한, 인증 수단 등)은 설정값으로 빼두고 기본값만 적용한다.
